---
apiVersion: v1
kind: Service
metadata:
  # name: "${APP_NAME}-${ROUTE_OFFER}"
  name: "account"
  namespace: "kube-public"
  labels:
    app: "account"
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: "account"
    
